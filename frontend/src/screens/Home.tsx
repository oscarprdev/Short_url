import HomeContainer from '../components/HomeContainer';

const Home = () => {
	return (
		<section className='flex flex-col h-full items-center gap-20 z-10 mt-32'>
			<HomeContainer />
		</section>
	);
};

export default Home;

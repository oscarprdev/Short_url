description: Returns user info
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 200
        user:
          $ref: 'components/_index.yaml#/schemas/User'
        urls:
          type: array
          items:
            $ref: 'components/_index.yaml#/schemas/Url'
